import { TeamButtonDirective } from './team-button.directive';

describe('TeamButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamButtonDirective();
    expect(directive).toBeTruthy();
  });
});
